import { createClient } from "@supabase/supabase-js"; import dotenv from "dotenv"; dotenv.config(); const url = process.env.SUPABASE_URL; const key = process.env.SUPABASE_SECRET_KEY; if (!url || !key) { console.error("Missing SUPABASE_URL or SUPABASE_SECRET_KEY"); process.exit(1); } const supabase = createClient(url, key); async function checkClient() { const email = "SIMILIAJORGE@gmail.com"; const { data: requests, error: reqError } = await supabase.from("song_requests").select("*").eq("email", email).order("created_at", { ascending: false }); if (reqError) { console.error("Error fetching requests:", reqError); return; } console.log("Found " + requests.length + " song requests for " + email + ":"); console.log(JSON.stringify(requests, null, 2)); for (const req of requests) { const { data: songs, error: songError } = await supabase.from("songs").select("*").eq("request_id", req.id); if (!songError && songs.length > 0) { console.log("\\nSongs for request " + req.id + ":"); console.log(JSON.stringify(songs, null, 2)); } } } checkClient();
